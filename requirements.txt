fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
aiosqlite>=0.20
pydantic>=2.9
pydantic-settings>=2.0

# Optional: PostgreSQL driver (uncomment to use Postgres instead of SQLite)
# asyncpg
# psycopg[binary]
