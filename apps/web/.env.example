# API base URL when the API lives on another origin. Leave empty when the
# Next.js rewrite (localhost:8000) is used.
#NEXT_PUBLIC_API_BASE=
# WebSocket origin override. Leave empty to use window.location.host.
#NEXT_PUBLIC_WS_HOST=localhost:8000