[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-dev-agent-core"
version = "0.1.0"
description = "Agent executor contract, registry and orchestration engine for AI Dev Office"
requires-python = ">=3.11"
dependencies = ["ai-dev-shared"]

[tool.setuptools]
packages = ["ai_dev_agent_core"]